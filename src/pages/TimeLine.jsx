import styled from "styled-components";
import TimeLineContainer from "../containers/TimeLine/TimeLineContainer";

const TimeLine = () => {
    return (
        <TimeLinePageBlock>
            <TimeLineContainer />
        </TimeLinePageBlock>
    );
};

const TimeLinePageBlock = styled.div``;
export default TimeLine;
